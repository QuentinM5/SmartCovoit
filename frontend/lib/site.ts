/** URL canonique du site — métadonnées, sitemap, robots.txt. Un seul endroit
 * à changer quand le domaine personnalisé remplace le sous-domaine workers.dev. */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";
